{
  "name": "root",
  "private": true,
  "scripts": {
    "clean-workspace": "workspace clean",
    "build-workspace": "build-workspace --skip=@proteinjs/server",
    "test-workspace": "workspace test --skip=@proteinjs/server",
    "watch-workspace": "watch-workspace"
  },
  "devDependencies": {
    "@proteinjs/build": "^1.0.15",
    "lerna": "8.1.2"
  }
}
